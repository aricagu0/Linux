# 터틀봇 버거 3대(리더 1 + 팔로워 2) + AR마커 리더-팔로워 프로젝트 설계

## 개요
터틀봇 버거(리더 1대 + 팔로워 2대, 총 3대)와 라즈베리파이4, AR 마커를 이용해 고정된
공간에서 SLAM 기반 자율주행과 마커 기반 로봇 추종을 결합한 프로젝트. 지정된 4개 좌표로
이동 명령을 내리면 선두 로봇(Leader)이 해당 좌표로 이동하고, 팔로워 로봇 2대는 각각
앞 로봇 후면에 부착된 AR 마커를 인식하여 **일렬로(체인 형태로)** 졸졸 따라간다.

```
Leader → Follower1 → Follower2
(리더 후면 마커를 F1이 추종) (F1 후면 마커를 F2가 추종)
```

리더-팔로워(Leader-Follower) 멀티로봇 구조로, 크게 5개 축으로 나눠서 설계한다:
① SLAM/맵핑, ② 리더 로봇 자율주행, ③ 팔로워 로봇 마커 추종(2대 체인), ④ 멀티로봇 통신/운영,
⑤ 안전/로깅/모니터링.

---

## 1. 전체 시스템 아키텍처

```
[고정 공간]
  └─ 선두 버거(Leader): 라이다 + Nav2로 4개 좌표 순차 이동
       └─ 후면에 AR 마커 #1 부착
  └─ 팔로워1(Follower1): 전면 카메라로 마커 #1 인식 → Leader 추종
       └─ 후면에 AR 마커 #2 부착
  └─ 팔로워2(Follower2): 전면 카메라로 마커 #2 인식 → Follower1 추종

[관제 PC 또는 Pi4 중 하나]
  └─ SLAM 맵 생성 (사전 1회) → 4개 좌표(Waypoint) 지정
  └─ 목표 좌표 명령 발행 → 리더에게 전달
```

핵심 설계 원칙:
- **리더는 지도 기반 절대 위치 제어(Nav2)**, **팔로워는 상대 위치 기반 로컬 제어(마커 추종)**
  로 완전히 다른 제어 방식을 사용한다. 이 둘을 섞지 않는 것이 중요하다.
- **팔로워끼리도 서로 다른 방식**: Follower1은 Leader를 추종하고, Follower2는 Follower1을
  추종한다. 즉 Follower1은 "리더" 겸 "팔로워" 역할을 동시에 수행 — 자기 자신은 Leader의
  마커를 보고 따라가면서, 동시에 자기 후면에 마커를 달아 Follower2에게 추종당하는
  **체인(chain) 구조**다.
- 마커는 로봇마다 **서로 다른 ArUco ID**를 부여해야 한다 (예: Leader=ID 1, Follower1=ID 2).
  그래야 Follower2가 실수로 Leader의 마커를 보고 따라가는 오류를 방지할 수 있다.

---

## 2. SLAM / 맵핑 단계

- **패키지**: `slam_toolbox` (turtlebot3 표준) 또는 `cartographer`
- 고정 공간이므로 **맵은 사전에 1회 작성 후 저장**(`map_server`로 정적 맵 사용)
  → 매번 SLAM을 돌릴 필요 없음. 리더 로봇만 이 맵으로 `AMCL` 또는
  `slam_toolbox localization mode`로 위치를 추정한다.
- 4개 좌표는 RViz2에서 `2D Pose Estimate` / `Nav2 Goal`로 좌표를 확인한 후,
  `(x, y, yaw)` 값을 코드나 waypoint yaml로 저장한다.

### 체크리스트
- [ ] 라이다가 장착된 로봇이 리더 역할이어야 함
      (팔로워는 라이다 없어도 무방, 카메라만 있으면 됨 — 단, 팔로워도 라이다가 있으면
      안전정지용 장애물 감지에 활용 가능)
- [ ] 맵 파일(`.pgm` / `.yaml`) 저장 및 두 로봇/PC가 공유

---

## 3. 리더 로봇 – 좌표 이동 (Nav2)

- `nav2_bringup` + `nav2_simple_commander` (Python API)로 4개 좌표를 순차적으로
  `NavigateToPose` 액션 호출
- 시나리오: "1번 좌표로 가라" 명령 → 리더가 Nav2로 이동 → 도착 시 팔로워에게
  "따라와" 트리거 또는 팔로워는 항상 추종 상태 유지(리더 정지 시 같이 정지)

```python
# 개념 예시
from nav2_simple_commander.robot_navigator import BasicNavigator
nav = BasicNavigator()
nav.goToPose(goal_pose_list[target_idx])
```

### 고려사항
- 좌표 이동 중 팔로워가 너무 붙거나 코너에서 마커를 놓치지 않도록
  **리더의 최대 속도 제한** 필요 (초반엔 0.1~0.15 m/s 권장)
- 리더 후면 마커가 회전/코너링 시 카메라 시야각(FOV) 밖으로 나가는 문제
  → 4번 항목에서 다룸

---

## 4. 팔로워 로봇 – AR 마커 추종 (2대 체인 구조, 가장 중요한 부분)

### 4-0. 팔로워 2대 체인 구조 설계 포인트
- **역할 분리**: Follower1의 노드 구성은 "마커 추종(구독)" + "자기 마커 상태 발행" 두 가지를
  동시에 갖는다. 반면 Follower2는 순수하게 "마커 추종"만 하면 된다.
- **ID 매핑 고정**: `marker_id=1 → Leader`, `marker_id=2 → Follower1` 로 고정하고,
  각 팔로워는 자신이 추종해야 할 ID만 필터링하도록 설정 (`ros2_aruco`는 여러 마커를
  동시에 인식하므로 인식된 마커 리스트에서 원하는 ID만 골라 쓰면 됨)
- **지연 누적(latency accumulation) 문제**: 체인이 길어질수록 Leader의 움직임이
  Follower2까지 전달되는 데 시간차가 생김 (Leader 회전 → F1 반응 → F2 반응 순으로 지연 누적)
  → Follower1·Follower2 모두 제어 루프 주기를 동일하게 맞추고, 목표 추종거리를
  Follower2 쪽을 약간 더 여유 있게(target_distance를 Follower1보다 5~10cm 더 크게) 설정하면
  급정지 시 추돌 위험을 줄일 수 있음
- **연쇄 정지(chain stop)**: Leader가 정지하면 F1도 거리 유지를 위해 정지 → F2도 자연히
  정지하게 되는데, 이 반응 지연 때문에 F2가 F1에 너무 붙는 경우가 생길 수 있음.
  목표거리·최소정지거리 파라미터를 F1보다 F2에서 더 넉넉하게 설정 권장

### 4-1. 마커 인식
- **라이브러리**: `ros2_aruco` (OpenCV ArUco 기반, 가장 무난) 또는 `aruco_opencv`
  — 여러 마커를 동시에 인식할 수 있으므로 팔로워 2대 구조에서도 라이브러리 교체 없이 사용 가능
- 마커 크기: 실측 거리 대비 인식 신뢰도가 중요
  → 로봇 간 거리 30~80cm 운용이면 **마커 한 변 8~10cm 이상** 권장
  (너무 작으면 원거리서 인식 불안정)
- 마커 부착 위치: 리더/Follower1 후면 **중앙, 지면과 수평, 뒤따르는 로봇의 카메라 높이와
  비슷한 높이**에 부착 (기울어지면 pose 추정 오차 큼). Leader와 Follower1 두 로봇 모두
  마커를 부착해야 하므로, 두 마커의 부착 높이를 동일하게 맞춰야 팔로워 카메라 설정을
  공용으로 쓸 수 있음

### 4-2. 카메라 → 로봇 좌표 변환
- ArUco pose estimation 결과는 카메라 좌표계 기준 `(x, y, z, roll, pitch, yaw)`
  → 이를 팔로워의 `base_link` 기준으로 정적 TF 변환 필요
  (카메라 마운트 위치/각도만큼 offset)
- 결과적으로 필요한 값: **마커까지 거리(depth, z값)**, **마커의 좌우 편차(x값 또는 화면상 각도)**

### 4-3. 추종 제어 로직 (PID 또는 단순 비례제어)
```
목표: 마커와의 거리 = target_distance (예: 40cm)
      마커의 화면 중심 대비 각도 = 0

linear.x  = Kp_lin * (measured_distance - target_distance)
angular.z = Kp_ang * (marker_angle_offset)
```
- **거리 오차 → 전진/후진 속도**, **각도 오차 → 회전 속도**로 분리한
  간단한 P/PID 제어로 충분히 동작 가능
- 마커를 잃어버렸을 때(occlusion, 급회전) 대응 로직 필수:
  - 일정 시간(예: 1~2초) 마커 미검출 시 **정지**
  - 최근 검출된 마커 방향으로 잠깐 회전 탐색(search behavior) 후 재검출 시도

### 4-4. 예상 문제점 (미리 설계에 반영)

| 문제 | 대응 |
|---|---|
| 코너링 시 마커가 카메라 FOV 밖으로 나감 | 리더 회전 속도 제한 + 팔로워 카메라 광각렌즈 사용 or 팔로워 추종거리 짧게 유지 |
| 조명 변화로 마커 인식률 저하 | 실내 조명 균일한 곳에서 테스트, 마커 코팅(무광) |
| 진동/모션 블러로 마커 인식 끊김 | 팔로워 이동 속도 상한, 카메라 노출시간/셔터스피드 조정 |
| 마커 인식 지연 → 후미 로봇이 밀림 | 제어 루프 주기 최소 10Hz 이상 확보, Pi4 카메라 처리 부하 확인 |
| Follower2가 잘못된 마커(Leader 마커)를 인식해 오작동 | 마커 ID 필터링 필수 적용, 인식 리스트에서 담당 ID만 사용 |
| 체인 지연으로 Follower2가 급정지 시 추돌 | Follower2의 target_distance를 Follower1보다 여유 있게 설정 |

---

## 5. 멀티로봇 통신/운영 구조 (로봇 3대 기준)

- ROS2는 로봇마다 `ROS_DOMAIN_ID`를 같게 설정하고, 각 로봇을 **네임스페이스 분리**
  (`/leader/...`, `/follower1/...`, `/follower2/...`)해서 토픽 충돌 방지
- 리더 상태(이동 중/도착)를 팔로워가 알아야 하면 간단한 상태 토픽(`/leader/status`)
  하나 두는 것도 방법 (다만 팔로워는 기본적으로 "마커 보이면 따라간다"만으로도
  충분히 구현 가능해서 필수는 아님)
- 로봇이 3대로 늘어나면서 Pi4 3대 + 관제 PC까지 **네트워크 트래픽/DDS discovery 부하가
  로봇 2대일 때보다 커짐** → Wi-Fi 안정성을 더 꼼꼼히 확인
  (DDS discovery 지연 문제 있으면 `CycloneDDS` + static peer 설정 고려,
  가능하면 로봇 전용 5GHz 공유기/AP 별도 구성 권장)
- 팔로워가 2대로 늘어나면서 **영상 스트리밍/카메라 토픽을 굳이 관제 PC로 보낼 필요는
  없음** — 각 팔로워는 자기 로컬에서 마커 인식 후 제어값만 계산하면 되므로, 네트워크에는
  좌표 명령/상태 토픽 정도만 오가도록 설계해서 부하를 최소화

---

## 6. 단계별 진행 로드맵 (제안, 팔로워 2대 기준)

1. **1단계**: 리더 로봇 단독으로 맵 작성(SLAM) → 저장, RViz에서 4개 좌표 확인/기록
2. **2단계**: 리더 로봇 Nav2로 4개 좌표 순차 이동 코드 완성 (팔로워 없이 단독 테스트)
3. **3단계**: 팔로워1 단독으로 정지된 리더(또는 고정 마커)를 향한 접근/정지 제어 튜닝
   (거리 유지 P제어)
4. **4단계**: 팔로워1 단독으로 마커를 든 사람/물체를 천천히 이동시키며 추종 테스트
   (동적 추종)
5. **5단계**: **팔로워2 추가** – 팔로워1과 동일한 로직을 팔로워2에 그대로 적용해
   단독으로 (팔로워1 또는 고정 마커를 대상으로) 추종 테스트, ID 필터링 정상 동작 확인
6. **6단계**: **2단 체인 테스트** – 팔로워1이 정지 상태일 때 팔로워2가 팔로워1을 잘
   따라가는지, 팔로워1이 움직일 때 팔로워2가 잘 쫓아가는지 (리더 없이 2대만) 확인
7. **7단계**: 리더+팔로워1+팔로워2 전체 통합 – 리더가 좌표 이동, 팔로워1·2가 순차적으로
   실시간 추종 (직선 구간부터)
8. **8단계**: 코너/회전 구간 포함해서 통합 테스트, 마커 미검출 시 정지/재탐색 로직 검증,
   체인 지연에 의한 추돌 여부 집중 점검
9. **9단계**: 전체 시나리오(4좌표 순회, 3대 동시 운용) 안정화 및 속도/거리 파라미터 튜닝

---

## 7. 안전/예외 처리 (팔로워 2대 구조에서 특히 중요)

- **비상 정지**: 각 팔로워가 바로 앞 로봇과 너무 가까워지면(예: 20cm 이하) 무조건 정지하는
  최소거리 로직 필수. Follower1→Leader, Follower2→Follower1 양쪽 모두 별도로 적용
- **장애물 회피**: 고정 공간이라도 사람이 지나가거나 예상 못한 물체가 있을 수 있음
  → 리더뿐 아니라 팔로워 2대도 라이다/초음파로 전방 장애물 감지 시 정지
- **체인 붕괴 대응**: Follower1이 마커를 놓쳐 정지했는데, Follower2가 그 사실을 모르고
  계속 접근하면 추돌 위험 → Follower2의 최소정지거리를 넉넉히 잡아 1차 방어선으로 삼기
- 리더가 Nav2 목표 도달에 실패했을 때(경로 막힘 등) 팔로워 2대가 계속 대기하도록
  처리하는 방안 마련

---

## 8. 성능 평가/로깅 (발표·보고서 자료로도 활용 가능)

- 팔로워별 **추종 오차 기록**: 목표 거리 대비 실제 거리, 각도 오차를 시간에 따라
  Follower1/Follower2 각각 CSV로 로깅 → 두 팔로워의 추종 성능을 비교 그래프로 제시 가능
- 마커 인식률(초당 프레임 대비 인식 성공률)을 팔로워별로 로깅
  → 체인 뒤쪽(Follower2)이 앞쪽(Follower1)보다 인식률이 낮아지는지 여부 확인 포인트
- 리더 Nav2의 목표 도달 시간, 경로 이탈 정도 기록
- **체인 지연 시간 측정**: 리더가 움직이기 시작한 시점부터 Follower1, Follower2가 각각
  반응하기 시작한 시점까지의 지연을 측정해두면 체인 구조의 한계를 정량적으로 보여줄 수 있음

---

## 9. 시각화/모니터링

- RViz2에서 **로봇 3대의 위치 + 마커 인식 상태를 동시에 표시**하는 대시보드 구성
- 각 팔로워 카메라 화면에 마커 인식 박스 + 거리/각도 값을 오버레이해서 디버깅용으로 확인
  (`cv2.putText` 등으로 간단히 가능), 팔로워1·팔로워2 화면을 동시에 띄워 비교하면 유용

---

## 10. 확장 가능성 (여유 있으면 고려)

- 팔로워가 앞으로 더 늘어날 가능성이 있다면, 마커 ID 매핑 규칙(`ID = N → Follower(N-1)이
  부착`)을 일반화된 패턴으로 설계해두면 확장이 쉬움
- 리더 로봇이 마커 대신 팔로워들에게 **경로 자체를 공유**하는 방식(웨이포인트 팔로잉)과
  비교 실험 → "왜 마커 체인 추종 방식을 택했는가"에 대한 비교 근거 자료로 활용 가능
- 마커가 안 보일 때 팔로워가 **자체 SLAM 맵**을 참고해 마지막 인식 위치로 이동하는
  하이브리드 방식 (난이도 높음, 시간 여유 있을 때)

---

## 11. 문서화 관점에서 추가하면 좋은 것

- **파라미터 표**: Follower1/Follower2 각각의 Kp_lin, Kp_ang, target_distance,
  최소정지거리 등 튜닝 값과 그 근거를 표로 정리 (보고서/발표 때 바로 활용 가능)
- **테스트 결과 표**: 직선 구간 vs 코너 구간, Follower1 vs Follower2에서의 추종
  성공률·오차를 비교

---

## 다음 확인 사항

가장 먼저 실험적으로 검증해야 할 부분은 **팔로워 카메라의 마커 인식 거리/각도 한계**
(3단계)이다. 이것이 확정돼야 리더의 이동 속도와 팔로워 추종거리 목표값이 정해진다.

- [ ] 라즈베리파이 카메라가 CSI 카메라인지 USB 웹캠인지 확인
  → 이에 따라 `ros2_aruco` 세팅 방식(카메라 드라이버, 캘리브레이션)이 달라짐
- [ ] Follower1용 마커(ID 2)와 Leader용 마커(ID 1)를 실제로 준비하고 인쇄
  → 두 마커가 뒤바뀌지 않도록 라벨링
- [ ] 팔로워 3대(Leader/F1/F2) 각각의 `ROS_DOMAIN_ID`, 네임스페이스 설정값 사전 확정
