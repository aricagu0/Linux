# ROS 2 환경 설정 스크립트 완벽 비교 가이드

ROS 2(Robot Operating System 2) 환경에서 가장 흔하게 접하게 되는 3가지 환경 설정 스크립트(`setup.bash`, `local_setup.bash`, `/opt/ros/.../setup.bash`)의 역할, 동작 차이, 그리고 활용법을 정리한 가이드 문서입니다.

---

## 📌 한눈에 보는 핵심 비교표

| 구분 | 1. `source /opt/ros/humble/setup.bash` | 2. `source ~/robot_ws/install/local_setup.bash` | 3. `source ~/robot_ws/install/setup.bash` |
| :--- | :--- | :--- | :--- |
| **불러오는 대상** | **순정 ROS 2 (기본 시스템)** | **내가 만든 코드/패키지만** | **기본 ROS 2 + 내가 만든 코드 둘 다** |
| **비유** | 📱 공장 출고 순정 OS | 🧩 내가 추가한 조각/모듈 | 📱+🧩 모든 앱이 활성화된 내 스마트폰 |
| **기본 ROS 2 명령어** | ⭕ 가능 (`ros2 run` 등) | ❌ 불가능 (미로드 시) | ⭕ 가능 |
| **사용자 작성 패키지** | ❌ **인식 불가** | ⭕ 가능 | ⭕ **가능** |
| **연쇄 로딩 (Chain)** | 없음 | 없음 (현재 위치만) | **있음 (상위 1번을 자동 로드)** |
| **주요 활용처** | ROS 2 기본 환경 단독 실행 | 빌드 도구(`colcon`) 내부 독립 처리 | **터미널 작업 시 (가장 권장)** |

---

## 🔍 상세 설명

### 1. `source /opt/ros/humble/setup.bash`
> **"순정 ROS 2 엔진을 켜는 기본 스위치"**

* **위치**: `/opt/ros/humble/` (리눅스 시스템 공용 디렉토리)
* **특징**:
  * 시스템에 공식 설치된 순정 ROS 2(Humble 버전) 전용 환경 설정 파일입니다.
  * `turtlesim`, `ros2 topic`, `ros2 node` 등 ROS 2 공식 기본 패키지만 불러옵니다.
  * **내가 직접 개발한 패키지(`py_pubsub`, `turtle_pkg` 등)는 인식하지 못합니다.**

---

### 2. `source ~/robot_ws/install/local_setup.bash`
> **"내가 만든 코드만 쏙 얹어주는 단품 스위치"**

* **위치**: `~/robot_ws/install/` (사용자 워크스페이스 설치 디렉토리)
* **특징**:
  * 내 워크스페이스 안에서 빌드된 패키지 환경변수만 독립적으로 등록합니다.
  * **상위 ROS 2 환경(`1번`)을 자동으로 불러오지 않습니다.**
  * 이미 터미널에 기본 ROS 2 환경이 적용되어 있을 때, 내가 수정한 패키지 경로만 효율적으로 갱신하기 위해 사용됩니다.

---

### 3. `source ~/robot_ws/install/setup.bash`
> **"1번 + 2번을 연쇄(Chain)로 불러오는 종합 선물 세트"** ★ *(권장)*

* **위치**: `~/robot_ws/install/` (사용자 워크스페이스 설치 디렉토리)
* **특징**:
  * 이 스크립트를 실행하면 **1번(기본 ROS 2)을 먼저 자동으로 불러온 후, 2번(내 워크스페이스 코드)을 그 위에 연달아 적용**합니다.
  * 기본 ROS 2 기능과 사용자 정의 로봇 패키지를 **모두 제약 없이 사용**할 수 있습니다.
  * 일반적인 터미널 작업 시 이 명령어 하나만 실행하면 됩니다.

---

## 💡 자주 묻는 질문 (FAQ)

### Q1. 개발할 때 터미널에서 무엇을 불러와야 하나요?
> **`source ~/robot_ws/install/setup.bash`** 하나만 부르시면 됩니다.  
> 상위 ROS 2 환경까지 자동으로 챙겨서 불러오므로 가장 안전하고 편리합니다.

### Q2. 이 파일들은 언제 생성되나요?
> 소스 코드 디렉토리(`src/`)에서 **`colcon build`** 명령어를 통해 빌드를 수행하면 `install/` 디렉토리 하위에 자동으로 생성됩니다.

### Q3. `.bashrc`에는 어떤 것을 등록하는 것이 좋은가요?
> 터미널을 열 때마다 매번 명령어를 입력하기 번거로우므로, `~/.bashrc` 파일 밑에 아래 구문을 추가해 두는 것을 추천합니다.
> ```bash
> source ~/robot_ws/install/setup.bash
> ```
